#ifndef TEXT_PARSER_HPP
#define TEXT_PARSER_HPP

#include <string>
#include <vector>

//Currently simple enough to not warrent references and pointers... Thats later
std::vector<std::string> parseUserInput(std::string a_userConsoleInput);


#endif //TEXT_PARSER_HPP